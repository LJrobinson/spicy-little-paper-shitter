# Spicy Little Paper Shitter

A tiny CLI receipt-printer chaos simulator for POS support survivors.

Built as a one-hour goblin project after job searching destroyed my higher brain function.

## Usage

```bash
dotnet run
dotnet run -- --ticket
dotnet run -- --receipt
dotnet run -- --fix
dotnet run -- --json
dotnet run -- --rage 10